import * as THREE from 'three';\n\nexport function createCarMaterial(\n  color: string,\n  metalness: number,\n  roughness: number,\n  options?: { clearcoat?: number; envMap?: THREE.Texture }\n): THREE.MeshStandardMaterial {\n  const material = new THREE.MeshStandardMaterial({\n    color,\n    metalness,\n    roughness,\n    clearcoat: options?.clearcoat || 0,\n    clearcoatRoughness: 0.1,\n  });\n\n  if (options?.envMap) {\n    material.envMap = options.envMap;\n  }\n\n  return material;\n}\n\nexport function createGlassMaterial(\n  transmission: number,\n  opacity: number,\n  envMap?: THREE.Texture\n): THREE.MeshPhysicalMaterial {\n  return new THREE.MeshPhysicalMaterial({\n    transmission,\n    opacity,\n    roughness: 0.05,\n    envMap,\n    toneMapped: false,\n  });\n}\n\nexport function lerpColor(\n  colorA: THREE.Color,\n  colorB: THREE.Color,\n  t: number\n): THREE.Color {\n  return colorA.clone().lerp(colorB, t);\n}\n