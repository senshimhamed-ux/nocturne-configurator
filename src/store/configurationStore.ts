import { create } from 'zustand';\n\nexport interface Configuration {\n  paintColor: string;\n  wheelStyle: string;\n  wheelFinish: string;\n  glassType: string;\n  interiorTheme: string;\n  trimLevel: string;\n  headlightsOn: boolean;\n  lightingMode: string;\n  cameraView: string;\n}\n\nconst defaultConfig: Configuration = {\n  paintColor: 'obsidian',\n  wheelStyle: 'sport',\n  wheelFinish: 'glossBlack',\n  glassType: 'clear',\n  interiorTheme: 'obsidian',\n  trimLevel: 'premium',\n  headlightsOn: false,\n  lightingMode: 'studio',\n  cameraView: 'hero',\n};\n\ninterface ConfigurationStore {\n  currentConfig: Configuration;\n  updateConfig: (updates: Partial<Configuration>) => void;\n  resetConfig: () => void;\n}\n\nexport const useConfigurationStore = create<ConfigurationStore>((set) => ({\n  currentConfig: defaultConfig,\n  updateConfig: (updates) =>\n    set((state) => ({\n      currentConfig: { ...state.currentConfig, ...updates },\n    })),\n  resetConfig: () =>\n    set(() => ({\n      currentConfig: defaultConfig,\n    })),\n}));\n